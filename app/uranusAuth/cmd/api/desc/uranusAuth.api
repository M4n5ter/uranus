syntax = "v1"

info(
	title: "auth"
	desc: "认证服务"
	author: "王勇涛 <wangyongtao2000@qq.com>"
	version: "v1"
)

import "verify/verify.api"

@server(
	group: verify
	prefix: auth/v1
)
service auth {
	@doc "验证 token"
	@handler token
	get /verify/token (VerifyTokenReq) returns (VerifyTokenResp)
}